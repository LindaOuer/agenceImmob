{% extends "base.html.twig" %}

{% block body %}
    <div class="container">
        <div class="row">
        <div class="col-md-8">
        
        </div>
        <div class="col-md-4">
            <h1>{{ property.title }}</h1>
            <h2> {{ property.rooms}} rooms - {{ property.surface }} m² </h2>
            <div class="text-danger"> {{ property.price | number_format(0, ' ', '') }} </div>

            <a href="#" class="btn btn-primary">Contact</a>
        </div>
        
    </div>
    <div class="row">
        <div class="col-md-8">

        <p>
            {{ property.description | nl2br }}
        </p>
            <table class="table table-striped">
                <tr>
                    <td>Surface</td>
                    <td> {{ property.surface }} </td>
                </tr>
                <tr>
                    <td>Rooms</td>
                    <td> {{ property.rooms }} </td>
                </tr>
                <tr>
                    <td>Floor</td>
                    <td> {{ property.floor }} </td>
                </tr>
                <tr>
                    <td>Bedrooms</td>
                    <td> {{ property.bedrooms }} </td>
                </tr>
                <tr>
                    <td>Heat</td>
                    <td> {{ property.heatType }} </td>
                </tr>
            </table>
        </div>
        <div class="col-md-4">
            <h2>Specificty </h2>
        </div>
    </div>
    </div>
{% endblock %}